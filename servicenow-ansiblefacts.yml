---
- name: Use cached facts to update ServiceNow CMDB 
  hosts: all 
  gather_facts: no

  tasks: 
    - debug: 
        msg: "{{ hostvars[groups['demo']] }}"

    # - name: Update CMDB with data from Ansible Facts  
    #   servicenow.itsm.configuration_item_batch: 
    #     sys_class_name: cmdb_ci_linux_server #cmdb_ci_ec2_instance 
    #     id_column_set: vm_inst_id 
    #     dataset: "{{ hostvars[groups['demo']] }}"
    #     map:
    #       vm_inst_id: machine_id 
    #       ip_address: public_ip_address 
    #       name: hostname
    #       mac_address: network_interfaces.mac_address
    #       state: state.name 
    #     instance: 
    #       host: https://{{ servicenow_instance }}.service-now.com
    #       username: "{{ servicenow_username }}"
    #       password: "{{ servicenow_password }}"    